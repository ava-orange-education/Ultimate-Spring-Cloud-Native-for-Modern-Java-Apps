/**
 * Student registry domain — maintains student master data (name, email, status).
 * <p>
 * Role in the monolith: core reference data used by enrollment and attendance.
 * Depends on no other domain module. Likely to remain in the core monolith during migration.
 */
package com.campusflow.student;
