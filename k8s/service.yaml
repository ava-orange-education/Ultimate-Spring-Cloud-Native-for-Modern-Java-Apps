apiVersion: v1
kind: Service
metadata:
  name: campusflow-monolith
  namespace: campusflow
spec:
  selector:
    app: campusflow-monolith
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
